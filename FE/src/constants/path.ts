const clientPaths = {
    home: '/',
}

export const { home } = clientPaths;


const adminPaths = {
    dashboard: '/admin'
}

export const { dashboard } = adminPaths;


const authPaths ={
    login: '/login',
    register: '/register',
}

export const { login, register } = authPaths;